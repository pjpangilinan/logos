# /data

Contains `aggregator.db` — the single committed SQLite database.

Generated and updated by `/scripts`, never hand-edited. Bundled into the
GitHub Pages deploy so the dashboard can load it client-side with `sql.js`.
